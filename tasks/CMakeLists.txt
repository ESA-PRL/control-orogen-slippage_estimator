# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(slippage_estimatorTaskLib)
ADD_LIBRARY(${SLIPPAGE_ESTIMATOR_TASKLIB_NAME} SHARED 
    ${SLIPPAGE_ESTIMATOR_TASKLIB_SOURCES})
add_dependencies(${SLIPPAGE_ESTIMATOR_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${SLIPPAGE_ESTIMATOR_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${SLIPPAGE_ESTIMATOR_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${SLIPPAGE_ESTIMATOR_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${SLIPPAGE_ESTIMATOR_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${SLIPPAGE_ESTIMATOR_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${SLIPPAGE_ESTIMATOR_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${SLIPPAGE_ESTIMATOR_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${SLIPPAGE_ESTIMATOR_TASKLIB_HEADERS}
    DESTINATION include/orocos/slippage_estimator)

